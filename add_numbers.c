#include <stdio.h>

// Program to add two numbers

int main()
{
    int a, b, sum;
    printf("Input integars: \n");
    scanf("%d%d", &a, &b);
    sum = a + b;
    printf("The total is - %d", sum);
    return 0;
}